## 2026-07-28

### API 配置：.env 文件就位
- 重建 `.env` 文件，填入用户真实 API Key（`AGENT_API_KEY=sk-XfQCvN...`）。
- `config.py` 已有 `load_dotenv()` + `Config._get_env_override()` 双层加载机制，无需改代码。

### README 重新整理
- 修复之前 PowerShell 写入导致的 7 处代码围栏损坏（``` 变成单反引号）。
- 修正教程第五步说明（`rag_pipeline.py` 只构建 768 主库，不构建 384 备用库）。
- 全文 5 处绝对路径改为 `<项目根目录>` 占位，压缩多余空行 1404→1008 行。
- 仅修改本地文件，未推送 GitHub。

### web_ui.py UI 优化
- 登录页 "首次使用请先注册账号" → "请输入用户名和密码"，去掉预设账号暗示。
- 右侧栏新增 "🔧 运维演示" 折叠面板（仅商家角色可见）：显示向量库状态、API 配置、速率限制、RAG 参数。
- `refresh_ops_panel()` 通过 `rag_pipeline` 模块级变量检查 ChromaDB 主/备库状态和向量数。
- 其他文件未动，未推送 GitHub。
- 验证通过：`config.get('API','api_key')` 正确返回 `.env` 中的密钥。

### 云端部署准备（Hugging Face Spaces）
- **目标平台**：HF Spaces（Gradio SDK），域名 `jiujiu369-<space名>.hf.space` 永久免费有效，满足"域名长期有效"要求。
- **新增 `app.py`**：云端入口，导入 web_ui.demo，启动时调用 `rag.build_vector_db_docs/goods()` 建向量库（云端 chroma_db 不上传，必须启动建库），再 `demo.launch()`。
- **`requirements.txt`**：补充 `python-dotenv`、`requests`，锁定 `gradio==6.20.0`。
- **`.gitignore`**：补 `datas/chroma_db_384/`，并 `git rm --cached` 取消跟踪本地生成的向量库（云端自建）。
- **云端模型加载**：`models/` 被 gitignore，云端无本地模型；靠 env `AGENT_RAG_EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5` / `AGENT_RAG_FALLBACK_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` 覆盖，由 `rag_pipeline._resolve_model_name` 触发从 HF 下载。已验证覆盖生效。
- **云端密钥**：`.env` 不提交，API Key 用 HF Space Secret（AGENT_API_KEY）注入。
- 已 commit + push 到 GitHub（jiujiu369/project1 master）。

### UI 文案配置化（已回退）
- 用户要求回退 ui_config.json 方案。`web_ui.py` 已恢复为内联常量版本，`ui_config.json` 已删除。

### 云端部署方案转向 Oracle Always Free VM
- **决策**：用户确认 HF Spaces 的 Gradio/Docker 需付费，改走 Oracle Cloud Always Free VM（4 OCPU / 24GB 内存 / 200GB 磁盘），全功能免费运行本地 embedding 模型（普通免费平台 512MB~1GB 装不下 1.5GB 模型）。
- **新增部署文件**（已 commit+push master）：
  - `Dockerfile`：python:3.11-slim 基础，装 gcc/g++，先装依赖再拷代码，挂载 `/app/datas` 与 `/app/.cache`（HF_HOME），ENTRYPOINT `python app.py`。
  - `.dockerignore`：排除 .venv/models/chroma_db/agent_memory/.env/deploy/test 等。
  - `docker-compose.yml`：端口 7860、env_file .env、注入模型名 env、命名卷持久化。
  - `deploy/setup-oracle.sh`：一键脚本（安装 Docker→克隆仓库→填 AGENT_API_KEY→开放 7860→可选 DuckDNS→docker compose up）。支持 `bash <(curl ...)` 远程执行。
  - `deploy/.env.template`：AGENT_API_KEY + base_url/model 默认值（apihub.agnes-ai.com/v1, agnes-2.0-flash）+ 云端模型名。
- **README 修正**：全文 `ui/gradio_app.py` 实际不存在，真实入口为 `web_ui.py`；已统一修正并新增"云端部署（Oracle VM）"章节。
- **注意事项**：Oracle 控制台 VCN 安全列表需手动开放 7860 入站；ARM(aarch64) 上 torch/sentence-transformers 有官方 wheel 支持。
- 真实 API 配置：`base_url=https://apihub.agnes-ai.com/v1`, `model_name=agnes-2.0-flash`（来自 config.py 默认值）。

### 部署约束更新：用户无境外信用卡
- **关键约束**：用户明确"没有境外信用卡" → Oracle VM（需绑卡验证）与 HF Spaces（Gradio 付费）均不可用。Cloudflare Pages / HF Static 为纯静态，跑不了 Python 后端，排除。
- **最终采用方案**：Cloudflare Tunnel 隧道穿透（免费、零信用卡）。本机已具备运行条件（模型/API Key/web_ui.py 全就绪），隧道把本地 7860 暴露到公网。
- **稳定域名**：命名隧道 `cloudflared tunnel create agent` → `cloudflared tunnel run --url http://localhost:7860 agent`，给 `https://<id>.cfargotunnel.com`（重启不变）。首次需 `cloudflared tunnel login`（免费 Cloudflare 账号，免卡）。
- **新增 `deploy/start_tunnel.bat`**：一键启动本地 web_ui.py + cloudflared 隧道。

### 安全加固（公网暴露前必做）
- **删除预设账号**：`tools/auth_login.py` 移除 `_DEFAULT_CONSUMER`/`_DEFAULT_MERCHANT` 常量；`init_auth_files()` 改为创建空文件（无预置）。`datas/consumer_users.json` 删 `user1`、`datas/merchant_users.json` 删 `admin`（保留 zkt/1111/2222 等真实注册账号）。`web_ui.py` 登录占位符与"预设账号"提示文案改为"首次使用请先注册账号"。
- **登录加频率限制**：`utils/rate_limiter.py` 新增 `_LOGIN_RATE_LIMITER`（10 次/60 秒）与 `rate_limit_login` 装饰器；`auth_login.login_user` 应用该装饰器，防公网暴力破解。
- **部署文档**：新增 `deploy/CLOUDFLARE_TUNNEL_GUIDE.md`（安装/登录/命名隧道/Cloudflare Access 前门/自定义域名/安全 checklist）+ `deploy/cloudflared-config.yml` 模板；`start_tunnel.bat` 改用命名隧道获稳定域名。
- 已验证：预设登录均失败、装饰器生效、三个文件语法通过、web_ui 导入正常。已 commit + push（fafe362）。

### README 重新整理（不推送 GitHub）
- 用户要求重新整理 README 且不推送。README 文件路径为 `F:/code/project1/README`（无 `.md` 后缀）。
- 修复之前 PowerShell here-string 写入遗留的字符损坏：教程章节 7 个代码块结束标记被损坏为单反引号，已全部恢复为三反引号（```）；全文代码围栏现成对（fence 偶数）。
- 澄清：`ase_url` "损坏"实为 `base_url` 子串误报，非真实损坏，无需修改。
- 通用化：全文 5 处硬编码本地绝对路径 `F:\code\project1\...` 改为 `<项目根目录>` 占位，使 clone 用户可直接照抄命令。
- 准确性修正：教程第五步原称"下载两个 embedding 模型/构建知识库向量"，但 `rag_pipeline.py` 的 `__main__` 与 `app.py` 仅构建 768 维主库；已改为准确说明（下载 768 主模型、构建主库，并指引「模型与向量库维护」章节构建 384 备用降级库）。
- 空行压缩：原文件段落间空行被撑长（3 连空行 191 处，共 1404 行），压缩为标准 1 个空行，总行数降至 1008，非空内容 505 行零丢失。
- 验证：fence 偶数、单反引号 0、独立 ase_url 无、绝对路径 0、第五步已更新。临时诊断脚本已清理。
- 按用户要求未推送 GitHub。
