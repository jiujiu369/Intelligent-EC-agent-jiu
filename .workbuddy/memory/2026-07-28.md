## 2026-07-28

### 删除云端部署相关文件与代码
- 删除磁盘文件 `.dockerignore`；`deploy/` 目录（.env.template、CLOUDFLARE_TUNNEL_GUIDE.md、cloudflared-config.yml、setup-oracle.sh、start_tunnel.bat）此前已不在磁盘，本次从 git 索引移除（git rm 暂存删除）。
- README 清理：删除首行"无法完成云端部署"提示段落，删除文件树中的 `app.py` 行（app.py 实际从未落盘）。
- 保留：`utils/api_monitor.py` 的 `CloudLLMClient`（调用云端 LLM 的核心功能，非应用部署代码）。
- 验证：全量 find + grep 无 docker/cloudflare/oracle/tunnel/app.py 残留。
- 注意：`.workbuddy` 目录此前已从磁盘移除（git 显示其下 memory 文件为 deleted），本次重建 memory 目录以恢复日志记录能力。未提交、未推送 GitHub。
