# 电商客服 Agent

基于 Python 的电商客服智能助手。项目通过 LLM Function Call 调用本地业务工具，提供登录、角色权限、商品与订单查询、售后工单、RAG 检索、会话记忆、日志、限流与回答风险检测能力。

> 本仓库是一个本地运行的课程/演示项目，不提供在线服务，也不提供预置账号或 API Key。

## 功能范围

| 模块 | 说明 |
| --- | --- |
| 登录与权限 | 买家、商家两种角色；支持注册、登录、忘记/找回密码、登录后修改密码和设置安全问题。 |
| 业务工具 | 商品、库存、订单查询；售后工单创建；商品更新；销售报表导出。 |
| RAG | 单个 512 维 BGE small 主向量库，并保留低内存的关键词降级检索。 |
| 会话 | 按 `role + username` 隔离会话；支持新建、切换、清空与超长会话摘要。 |
| 界面 | CLI 与 Gradio 图形界面；买家、商家分别显示对应能力说明和示例，商家可查看运维状态；Web UI 支持终止当前 Agent 请求。 |
| 运行保障 | 环境变量配置、限流、缓存、日志、输入校验与 API 异常兜底。 |
| 回答风险检测 | 基于工具返回字段和过度承诺词的规则检测，详见下文边界说明。 |

## 角色与权限

| 角色 | 可用工具 |
| --- | --- |
| 买家 | `rag_search`、`query_goods`、`query_order`、`create_aftersale_ticket` |
| 商家 | 买家全部工具，以及 `query_stock`、`update_goods`、`export_sales_report` |

权限控制位于 `tools/rbac.py`：`get_filtered_schemas()` 按角色过滤 LLM 可见的 Function Schema，`check_permission()` 在工具执行前再次校验。买家商品查询结果会隐藏 `上架状态` 字段；订单查询还会在工具执行层强制注入当前登录用户名作为 `用户ID`，因此买家只能查询自己的订单，商家可以查询全部订单。

## RAG 检索与降级

实现位于 `embedding/rag_pipeline.py`。

1. 优先查询 512 维主向量库（`datas/chroma_db_512`）。
2. 主库异常、无结果或相似度过低时，尝试关键词降级检索。
3. 关键词检索仍无结果时，返回“暂未找到相关知识，建议联系人工客服”。

主模型为 `BAAI/bge-small-zh-v1.5`（512 维）；`models/` 下存在同名目录时优先使用本地模型。

## 回答风险检测：适用范围与限制

实现位于 `tools/hallucination_checker.py`。该模块是**规则型回答风险检测**，不是通用大模型“幻觉率”评估，不能据此推导或宣称通用幻觉率降低。

它会：

- 从回答中提取商品名、订单号和金额，并与当前工具返回数据比对；
- 检测 `绝对`、`肯定`、`保证`、`100%` 等过度承诺词；
- 将问题按规则计分，分数大于 `0.3` 时标记为风险；
- 在单次运行的进程内统计当前会话的风险占比。

它不会评估事实真实性、上下文遗漏、知识库覆盖率、语言表达错误或未调用工具时的所有潜在不实回答。要报告通用幻觉率，需另行定义数据集、标注准则、分母、评审流程与统计方法。

## 环境要求

- 请使用项目虚拟环境运行；本 README 的测试记录使用 Python 3.14.6，其他 Python 版本的兼容性需在目标环境中验证。
- 首次构建向量库需要网络访问以下载模型；如模型已在 `models/` 目录中，可离线复用。
- 支持 Windows、macOS、Linux；以下命令以 PowerShell 为例。

## 快速开始

```powershell
git clone https://github.com/jiujiu369/Intelligent-EC-agent-jiu.git
cd Intelligent-EC-agent-jiu

python -m venv .venv
.\\.venv\\Scripts\\Activate.ps1
python -m pip install -r requirements.txt
```

在项目根目录创建 `.env`，并按实际服务商填写：

```dotenv
AGENT_API_KEY=你的_API_Key
AGENT_BASE_URL=https://你的服务商地址/v1
AGENT_MODEL_NAME=你的模型名
```

也可在启动前通过环境变量设置：

```powershell
$env:AGENT_API_KEY="你的_API_Key"
$env:AGENT_BASE_URL="https://你的服务商地址/v1"
$env:AGENT_MODEL_NAME="你的模型名"
```

配置主嵌入模型：

```dotenv
AGENT_RAG_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
AGENT_RAG_PRIMARY_MODEL=BAAI/bge-small-zh-v1.5
```

构建主向量库：

```powershell
python embedding\\rag_pipeline.py
```

启动图形界面或 CLI：

```powershell
python web_ui.py
python agent\\main_agent.py
```

Gradio 默认地址为 `http://127.0.0.1:7860`。

Web UI 提交消息后会立即显示用户消息，原位置的“发送”按钮会切换为红色“终止”按钮。点击“终止”会结束并回收当前 Agent 工作进程；请求从正式提交起超过 60 秒仍未完成时，后端也会自动终止并回收该进程。该机制用于本地课程/演示项目的单次请求控制，不代表已经完成生产级并发能力或线上部署验证。

首次使用请自行注册账号。账号文件位于 `datas/consumer_users.json` 与 `datas/merchant_users.json`，密码以 PBKDF2-SHA256 加盐哈希保存。

## 配置

统一配置文件为 `config.py`，支持以下环境变量覆盖：

| 环境变量 | 覆盖项 |
| --- | --- |
| `AGENT_API_KEY` | `API.api_key` |
| `AGENT_BASE_URL` | `API.base_url` |
| `AGENT_MODEL_NAME` | `API.model_name` |
| `AGENT_RAG_PRIMARY_MODEL` | `RAG.embedding_model` |
| `AGENT_RAG_DISTANCE_THRESHOLD` | `RAG.distance_threshold` |

不要将真实 API Key 提交到仓库。

## 测试

一键运行全部测试：

```powershell
.\\.venv\\Scripts\\python.exe run_all_test.py
```

### 最近一次复现结果

运行时间：2026-09-02；解释器：Python 3.14.6；命令：`.\\.venv\\Scripts\\python.exe run_all_test.py`。

| 项目 | 结果 |
| --- | --- |
| 发现的测试脚本 | 23 |
| 通过 | 23 |
| 失败 | 0 |

测试覆盖账号安全、买家/商家 RBAC、买家订单隔离、单 512 维主向量库初始化与重建、空回复保护、配置优先级、UI 账号辅助逻辑、日志、限流和回答风险检测。测试会写入部分临时状态文件，运行前仍建议备份需要保留的本地账号、会话或业务数据。

## 目录结构

```text
Intelligent-EC-agent-jiu/
├─ agent/                 # CLI Agent 与会话逻辑
├─ embedding/             # RAG 与向量库维护
├─ tools/                 # 鉴权、业务工具、提示词、风险检测
├─ utils/                 # API 客户端、日志、限流缓存
├─ datas/                 # 业务数据与向量库
├─ models/                # 本地 embedding 模型
├─ agent_memory/          # 本地会话记录
├─ test/                  # 可独立运行的测试脚本
├─ config.py
├─ run_all_test.py
├─ web_ui.py
└─ requirements.txt
```

`agent_memory/` 中可能包含本地测试产生的历史会话；如不需要可在退出程序后自行清理。
