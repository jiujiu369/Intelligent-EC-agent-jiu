# 电商客服 Agent —— 项目文件说明书

> 适用项目：`F:\code\project1`（基于 LLM Function Call + RAG 的本地电商客服助手）
> 文档用途：逐文件说明作用、关键函数与设计意图，便于维护与二次开发。

---

## 0. 系统总览

项目分为五层：

| 层 | 目录 / 文件 | 职责 |
| --- | --- | --- |
| 入口层 | `web_ui.py` / `agent/main_agent.py` | CLI 与 Gradio 两个运行入口 |
| 编排层 | `agent/main_agent.py` | 对话循环、会话管理、工具调度、权限与脱敏编排 |
| 工具层 | `tools/` | 业务工具（商品/订单/库存/售后/报表）、登录鉴权、RBAC、提示词、幻觉检测、错误处理 |
| 支撑层 | `utils/` | LLM 客户端、日志、限流/缓存 |
| 知识层 | `embedding/rag_pipeline.py` + `datas/` + `models/` | 向量库构建与检索、业务数据、Embedding 模型 |
| 评测层 | `eval/` | 幻觉率对比评测 |
| 测试层 | `test/` + `run_all_test.py` | 单元/集成测试与一键批量执行 |

关键数据流：用户输入 → 输入校验 → 角色过滤 Schema → LLM（云端）→ 工具调用（二次鉴权 + 脱敏）→ RAG/业务数据 → 幻觉检测 → 回答 + 记忆落盘。

---

## 1. 根目录文件

### `README`
项目说明文档：功能范围、角色权限矩阵、RAG 降级策略、回答风险检测边界、环境要求、快速开始命令。是新人第一阅读入口。

### `requirements.txt`
Python 依赖清单。核心：`gradio`（Web UI）、`chromadb`（向量库）、`sentence-transformers`（Embedding）、`fastapi`/`uvicorn`（预留）、`pydantic`、`PyPDF2`/`python-docx`（文档解析）、`jieba`（关键词降级检索）、`python-dotenv`/`requests`（配置与 API）。

### `config.py`
**全局配置与路径中心**。定义并导出：
- `API`：API Key、Base URL、模型名、超时、重试、温度。
- `PATHS`：所有数据/日志/记忆目录的相对路径（用户数据 JSON、两个向量库目录、记忆目录、日志目录）。
- `RAG`：主模型与备用模型均为 `bge-base-zh-v1.5`（768 维），分块大小 384、重叠 64、距离阈值 1.5。
- `SESSION` / `AGENT`：会话轮次、摘要保留轮次、最大工具调用轮次 5、温度 0.1。
- `_model_path()`：模型路径解析（优先 `.env` 覆盖 → 本地 `models/` → 相对路径）。
- `Config` 类与 `get(section, key, default)`：支持环境变量 `AGENT_<KEY>` / `AGENT_<SECTION>_<KEY>` 覆盖配置。
> 所有模块均通过 `config.get(...)` 取路径，**改动目录结构只需改这里**。

### `.env`
本地环境变量（不入库）。配置 `AGENT_API_KEY`、`AGENT_BASE_URL`、`AGENT_MODEL_NAME`。`config.py` 启动时自动 `load_dotenv()` 加载。

### `web_ui.py`
**Gradio 图形界面入口**（`python web_ui.py` → `http://127.0.0.1:7860`）。复用 CLI 全部业务逻辑，仅做界面封装。包含：
- 登录/注册视图，主聊天视图，会话管理侧栏（新建/切换/清空/刷新）。
- `refresh_ops_panel()`：商家专属运维面板，展示主/备用向量库状态、API 配置、限流参数、RAG 参数。
- 内置 API Key 缺失告警（`API_KEY_WARN`）。
- 顶部注释含端口冲突排查命令。

### `run_all_test.py`
**一键批量测试器**。扫描 `test/` 下所有 `test*.py`，逐个子进程运行并汇总 PASS/FAIL、耗时与失败日志。与 `test/test_run_all_test.py` 形成自测闭环。

### `image.png` / `image-1.png` / `image-2.png`
界面或文档中使用的占位/示例图片（非代码逻辑，属资源文件）。

---

## 2. `agent/` 目录

### `agent/main_agent.py` —— 核心编排器
整个 Agent 的大脑。关键能力：
- **会话管理**：`list_sessions` / `load_memory` / `save_memory` / `clear_memory` / `clear_all_memory`，按 `role_username` 隔离目录，自动编号会话名（对话一、对话二…）。
- **记忆摘要**：`summarize_memory()` 超出 `max_message_rounds` 时把旧对话压缩为 system 摘要，控制上下文长度。
- **主循环 `_run_agent()`**：
  1. `validate_user_input` 输入校验；
  2. 命中重复提问缓存直接返回；
  3. 加载历史记忆并拼装系统提示词；
  4. `get_filtered_schemas()` 按角色过滤工具 Schema（第一层防护）；
  5. 循环调用云端 LLM，解析 `tool_calls`；
  6. `validate_tool_args` 参数校验 → `check_permission` 二次鉴权（第二层防护）→ `mask_tool_result` 数据脱敏 → 执行；
  7. 达到最大轮次（5）兜底退出。
- **回答收尾 `_finalize_answer()`**：调用 `hallucination_checker` 做风险检测，记录重复提问缓存。
- `__main__`：交互式 CLI 入口，含登录、会话命令（新建/切换/重新登录/清空）与帮助菜单。
> 依赖：`config`、`utils.api_monitor.llm_client`、`utils.rate_limiter`、`tools.*`、`utils.logger`。

---

## 3. `tools/` 目录

### `tools/schema.py` —— 工具清单与调度表
- `tool_schemas`：全部 7 个工具的 OpenAI Function Calling Schema（名称、描述、参数、必填项）。供 LLM 选择调用。
- `func_mapping`：工具名 → 实际函数 的映射表（调度核心）。其中 `query_goods`/`rag_search` 套了限流缓存装饰器。
- 工具清单：`rag_search`、`query_goods`、`update_goods`、`query_stock`、`query_order`、`export_sales_report`、`create_aftersale_ticket`。

### `tools/data_loader.py` —— 业务数据层
- 启动即 `init_data()` 把商品/库存/订单/售后 JSON 载入内存（全量缓存）。
- 业务函数：`query_goods`（按 ID/名称查）、`update_goods`（改商品，含价格别名映射）、`query_stock`、`query_order`（按订单号/商品/时间范围）、`create_aftersale_ticket`（含中英文字段别名映射与必填校验）、`export_sales_report`（按时间统计销售额）、`get_all_orders`。
- 每个函数经 `_guard_tool_result` + `safe_tool_call` 包裹，空结果转为“未找到匹配信息”，异常安全返回。
- 写操作通过 `error_handler.atomic_save_json` 持久化。

### `tools/auth_login.py` —— 登录与鉴权
- 密码采用 **PBKDF2-SHA256 + 32 字节随机盐** 哈希，绝不存明文；恒定时间比较防时序攻击。
- `init_auth_files()`：自动创建 `consumer_users.json` / `merchant_users.json`（空文件，无预设账号）。
- `register_user` / `login_user`：注册与登录校验，用户名/密码规则校验，失败返回模糊提示（不泄露账号是否存在）。
- `auth_interactive()`：CLI 交互式登录/注册流程；`_getpass()` 跨平台隐藏密码回显（Windows 用 msvcrt）。
- `@rate_limit_login`：登录限流（60s/10 次）。

### `tools/rbac.py` —— 角色权限控制（双层防护）
- `ROLE_PERMISSIONS`：买家 4 个工具，商家 7 个工具（额外含库存/改商品/报表）。
- `get_filtered_schemas()`：**第一层防护**——只把当前角色允许的工具 Schema 传给 LLM。
- `check_permission()`：**第二层防护**——工具执行前二次校验，越权直接拦截返回“权限不足”。
- `mask_goods_data` / `mask_tool_result`：**数据脱敏**——买家查商品时隐藏“上架状态”等内部字段。
- `get_role_prompt_suffix()`：把角色与可用工具写进系统提示词。

### `tools/prompt_manager.py` —— 系统提示词模板
- `PROMPT_TEMPLATES`：基础身份、买家/商家角色描述、Function Call 约束、反幻觉约束、异常兜底话术。
- `build_system_prompt(role, session_context)`：按角色拼装系统提示词，并追加历史摘要上下文。
- `get_error_message(error_type)`：统一异常兜底文案。

### `tools/hallucination_checker.py` —— 规则型回答风险检测
> 注意：这是**规则型风险检测**，不是通用大模型幻觉率评估（README 已声明边界）。
- 从回答中提取订单号、金额、商品名，与工具返回数据比对。
- 检测“绝对/肯定/保证/100%”等过度承诺词。
- 按规则计分，>0.3 标记风险；`get_session_stats()` 返回当前进程内会话风险占比。
- 不评估事实真实性、上下文遗漏、知识库覆盖率。

### `tools/error_handler.py` —— 错误处理与数据可靠性
- 输入校验：`validate_user_input`（空/超长截断 500 字/无意义短语/不可打印字符过滤），返回结构化 `InputValidationResult`。
- 参数校验：`validate_tool_args` 必填项检查。
- 工具结果包裹：`wrap_tool_result`（空列表转“未找到”）、`safe_tool_call`（异常安全）、`safe_rag_call`（RAG 异常转兜底）。
- 兜底文案常量：`LOST_MESSAGE`、`BUSY_MESSAGE`、`RAG_FALLBACK_MESSAGE` 等。
- JSON 原子读写：`atomic_load_json` / `atomic_save_json`（按文件加锁，避免并发写损坏）。
- 会话文件修复：`recover_memory_file`（损坏自动重建为空）。
- 记忆摘要：`summarize_memory()`（旧对话压缩为 system 摘要）。
- RAG 阈值过滤：`filter_rag_results` / `rag_fallback_result`。

---

## 4. `utils/` 目录

### `utils/api_monitor.py` —— 云端 LLM 客户端
- `CloudLLMClient.chat_completion()`：调用 OpenAI 兼容 `/chat/completions`，支持 `tools`/`tool_choice=auto`。
- 完整异常处理与重试（`max_retry=2`）：超时、401（密钥失效）、402（余额不足）、429（限流，读 `Retry-After`）、5xx（退避重试），最终均返回兜底响应而非抛异常。
- 全局单例 `llm_client`：项目统一导入，配置取自 `config.get("API", ...)`。
- 日志脱敏：请求日志中 Authorization 显示为 `***`，并记录 token 估算。

### `utils/logger.py` —— 日志系统
- `get_logger(name)`：模块级统一日志入口。
- `_DailyFileHandler`：按天滚动写文件 `logs/YYYY-MM-DD.log`，自动清理 30 天前日志。
- `set_console_logging_enabled()`：运行时开关控制台日志（Web UI 启动时关闭控制台噪声）。
- 日志格式含 `session_name` 字段（通过 `_SessionFilter`），便于按会话追溯。

### `utils/rate_limiter.py` —— 限流与缓存
- `TokenBucketRateLimiter`：令牌桶限流。API 限流 30 次/60 秒；登录限流 10 次/60 秒（`rate_limit` / `rate_limit_login` 装饰器）。
- `ttl_cache`：带 TTL 的缓存装饰器。`cache_query_goods`（5 分钟）、`cache_rag_search`（10 分钟）减少重复查询开销。
- `RepeatedQueryDeduplicator`：重复提问去重，5 秒内同一会话相同问题直接返回上次答案（`get_repeated_query_answer` / `remember_query_answer`）。

---

## 5. `embedding/` 目录

### `embedding/rag_pipeline.py` —— RAG 检索管道
- **模型与向量库初始化**：启动时分别加载主库与备用库（均为 768 维 bge），失败标志位 `chroma_connection_failed` / `fallback_chroma_connection_failed`。
- **文档解析**：`load_document()` 按后缀分派 `load_txt_md` / `load_pdf` / `load_docx`，`clean_text()` 清洗 Markdown 噪声。
- **文本切分**：`split_text()` 滑动窗口（chunk_size 384，overlap 64）。
- **入库**：`build_vector_db_docs()` / `build_vector_db_goods()`（政策文档 + 商品信息，带去重 `_add_chunks_if_missing`）。
- **检索 `rag_search()`**：主库优先 → 主库异常/无结果转备用库 → 仍无果转 `fallback_keyword_search()`（jieba + TF-IDF 关键词降级）→ 全失败返回“联系人工客服”兜底。
- **维护**：`update_single_goods_vector()`（单商品更新）、`rebuild_all_vectors()`（全量重建并支持失败回滚 `_restore_collection_snapshot`）、`clear_all_goods_vector()`。
- 阈值过滤：`_search_collection()` 按 `distance_threshold=1.5` 过滤低相似度片段。

---

## 6. `eval/` 目录

### `eval/evaluate.py`
**幻觉率对比评测脚本**。对 `eval_set.json` 中 `scope=out`（知识库外）问题，分别用「完整管线 `run_agent`」与「裸 LLM（无 RAG/无工具）」回答，用内置 `hallucination_check` 判定是否幻觉，输出两组幻觉率与降幅。结果可用于量化 RAG + 兜底策略的效果。

### `eval/eval_set.json`
评测数据集。`description` 说明字段；`questions` 数组含 `q`（问题）与 `scope`（`out` 知识库外应兜底 / `in` 正常业务）。`eval/evaluate.py` 默认只取 `out` 类问题评测。

---

## 7. `datas/` 目录（数据与知识库）

| 文件 / 目录 | 作用 |
| --- | --- |
| `货品基础数据.json` | 商品主数据（名称/ID/分类/规格/售价等），业务查询与向量化来源 |
| `库存数据.json` | 商品库存，商家查询 |
| `订单数据.json` | 订单记录，按订单号/商品/时间查询与报表统计 |
| `售后工单.json` | 售后工单存储，创建后追加持久化 |
| `consumer_users.json` / `merchant_users.json` | 买家/商家账号（仅哈希密码+盐），由 `auth_login` 维护 |
| `datas/docs/` | 售后政策等文档（.md/.txt/.pdf/.docx），RAG 政策库来源 |
| `datas/chroma_db` | 主向量库（768 维）持久化目录，ChromaDB 自动管理 |
| `datas/chroma_db_fallback_768` | 备用向量库（768 维）持久化目录 |
| `datas/测试题.txt` | 测试/演示用问题清单 |

> 向量库目录（`chroma_db*`）由 ChromaDB 自动生成，不要手动编辑。

---

## 8. `agent_memory/` 目录
会话记忆存储。按 `角色_用户名/` 分子目录，每个会话一个 JSON（如 `consumer_1111/对话一.json`）。由 `main_agent` 的 `save_memory`/`load_memory` 读写，含自动摘要压缩。示例子目录：`consumer_1111`、`merchant_1111`、`consumer_eval` 等。

---

## 9. `models/` 目录
本地 Embedding 模型权重（首次运行需联网下载，之后离线复用）：
- `bge-base-zh-v1.5`：主模型，768 维，对应 `datas/chroma_db`。
- `bge-base-zh-v1.5`：主、备用模型均为 768 维，分别对应两个独立向量库目录。
- `qwen2.5-vl`：视觉语言模型目录（当前管线未直接调用，预留）。
> 两个模型维度不同，**必须使用独立 ChromaDB 目录**，否则向量维度不匹配。

---

## 10. `logs/` 目录
运行日志。按天滚动（`YYYY-MM-DD.log`），并含 Gradio UI 的 `gradio_ui.out.log` / `gradio_ui.err.log` 及带端口号的变体（如 `gradio_ui_7861.*.log`）。由 `utils/logger` 自动清理 30 天前文件。

---

## 11. `test/` 目录
单元测试与集成测试（`test*.py`），覆盖：
- 业务逻辑：`test_auth.py`、`test_business_tools.py`、`test_rbac.py`、`test_prompt_manager.py`、`test_hallucination_checker.py`、`test_config.py`、`test_context_window.py`、`test_edge_cases.py`。
- 运行保障：`test_rate_limiter.py`、`test_api_monitor.py`、`test_error_handler.py`、`test_logger.py`、`test_logging_integration.py`、`test_agent_error_integration.py`、`test_agent_rate_limiter_integration.py`。
- RAG：`test_rag.py`、`test_rag_pipeline_fallback.py`、`test_rag_vector_maintenance.py`。
- 自测：`test_run_all_test.py`（验证批量测试器本身）。

---

## 12. 其他

- `__pycache__/`：Python 编译缓存（自动生成，无需关注，`.gitignore` 应忽略）。
- `.git/`：版本控制目录。
- `.venv/`：虚拟环境（README 建议 `python -m venv .venv` 创建）。

---

## 13. 快速运维指引

```powershell
# 安装依赖
python -m venv .venv; .\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt

# 首次构建向量库
python embedding\rag_pipeline.py

# 启动界面 / CLI
python web_ui.py
python agent\main_agent.py

# 跑全部测试
python run_all_test.py

# 跑幻觉率评测
python eval\evaluate.py
```

**关键修改点速查**：路径/模型 → `config.py`；工具增删 → `tools/schema.py`（Schema+映射）；权限变更 → `tools/rbac.py`；提示词 → `tools/prompt_manager.py`；业务数据字段 → `tools/data_loader.py` + `datas/*.json`。
